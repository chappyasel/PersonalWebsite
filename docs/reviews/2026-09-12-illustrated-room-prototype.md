The illustrated shelf can be the site's usable first view and its lasting fallback when WebGL is unavailable. The prototype now supports navigation across all seven approved drawings and opens each shelf's existing content. It has no renderer dependency and no permanent loading message.

Open [the illustrated room](http://localhost:3330/admin/room-boot-comparison?variant=illustrated&unit=projects). This development route is available on the root server at port 3330. The original artwork preview remains available on the same route without `variant=illustrated`.

The camera experiment lives separately in the renderer builder's worktree on port 3322. It has demonstrated mounted-scene registration on Talks at 1440 by 900 and Weightlifting at 390 by 844. Each case compares four shelf vertices and four interior photograph centers against the saved capture transforms. Both pass the 3 CSS pixel gate. These are geometric correspondences, not a claim that every shaded pixel equals the illustration. Root inspected the saved illustration, matched scene and final room images.

The transition keeps the camera still while the illustration dissolves over 160 milliseconds. Camera travel starts after that dissolve and takes 400 milliseconds. Scene navigation and content panels stay hidden during the move, then return at the ordinary room pose. The phone capture records no camera movement while the illustration remains visible. The scene uses the existing canvas. This development experiment still needs consolidation with the usable content view before production integration.

Successful captures used software Chromium with the existing no-composer diagnostic and meadow disabled. Normal visual effects have not passed a complete matching run after the camera correction. These captures establish geometry and ordering, not production smoothness or latency. Reading, resize and navigation cancellation pass. Active context-loss recovery has only a source review; the browser dispatched context loss after entering 2D, which does not prove that transition. The [camera result](./illustrated-room-promotion-RESULT.md) records the limits and links the saved evidence. Camera implementation remains in the `prototype/boot-render-masks` worktree on port 3322.

Production should use one section selection and one content system across both renderings. Serve the selected drawing immediately, then start the existing room only when its capability and preference policy permits. Promote after the selected shelf has actually rendered at the registered pose. Once someone opens content or starts navigating, keep their view stable and make entering 3D their choice. An unavailable renderer should leave the illustrated room usable. A lost context should preserve the selected shelf and open content when returning to 2D. Reduced motion and SaveData retain their current policy.

Root verified all seven shelves in light desktop and dark phone views with WebGL explicitly blocked. All fourteen cases have zero WebGL calls, canvas elements and 3D model requests. Existing content and links render, controls fit the viewport, and keyboard opening, Escape and focus restoration work. Additional checks cover About's embedded theme toggle, section hash links, browser Back, failed initial texture downloads and a failed SVG fetch. Failures leave Explore available. Targeted TypeScript and ESLint passed. Reproduction scripts and results are in [illustrated-room-ui-evidence](./illustrated-room-ui-evidence/).

The initial theme query is applied once. Installed next-themes changes its setter identity with the theme; reapplying the query whenever that setter changes caused an update loop. The prototype now guards initialization and uses the shared theme provider for subsequent changes. No production dialog or theme component needed modification.

The approved artwork, original About boot source and owner's plan remain unchanged. This is a development prototype. Production integration still needs route-selected artwork, correct theme and responsive framing on first paint, shared content and interaction state, and a decision about illustrated-object hotspots. The existing semantic document remains the no-JavaScript path. The live camera proof currently covers two shelves. Extending it to the other five requires checking their mounted geometry and animated details, including clock hands, against their illustrations.

No Field Notes discovery is added. Automatic fallback or promotion fails quality-bar test 2 because it is not a meaningful visitor action. The development preview itself adds no public discovery.
