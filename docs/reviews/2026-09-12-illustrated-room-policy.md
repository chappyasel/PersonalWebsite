The existing world boot machine now owns illustrated delivery and promotion. Legacy starts remain unchanged unless `illustratedMode: true`; the browser adapter reads that opt-in from `data-room-illustration="enabled"`. OG capture opts out. The page must establish the marker before its prepaint script runs and clear it for legacy routes.

`WorldBootView` adds `presentation`, `illustrationKey`, `interactionHeld`, `handoffStartedAt`, `motionEnabled`, `canRequest3D` and `canvasVisible`. Presentation advances from illustrated to dissolve, travel and live. Existing asset, first-frame and meadow gates remain required. Matching registration starts a 160ms dissolve; the machine permits travel after 180ms. The renderer owns its 400ms travel and reports actual arrival. Only that arrival publishes `revealed` and `data-world="ready"`, records warm delivery and allows completed-room residency. `data-room-view` mirrors presentation and disappears while the room is parked.

The scene reports epoch-bound `illustrationRegistered`, `illustrationTravelCompleted` and `illustrationUnavailable`, each carrying the current string key. Registration means the renderer observed at least two actual matching frames. Keys must identify the selected artwork, revision, theme and viewport; the UI sends `illustrationChanged` with a new key or null when that contract changes. A mismatch cannot promote. Registration failure and transition timeout return the usable illustration.

The UI sends `illustrationInteracted` when reading or navigation begins. During loading or handoff this releases the renderer and holds the illustrated view. During live 3D it only holds future promotion. The latch survives hydration and context-loss restarts, including interaction between an armed recovery timer and its callback. A true exit or `worldBoot.request3D()` clears it. Explicit requests recheck capability and preferences and respect the context-loss recovery budget. Reduced motion and SaveData retain semantic document delivery. Unavailable WebGL and runtime failure use the illustration.

`worldBoot.setIllustrationMotionEnabled(false)` disables registration and handoff requirements. The ready ordinary camera can reveal directly. The renderer adapter must restore or retain that ordinary camera and avoid matching work in this mode. The override lasts only for the current JS session and does not alter quality or preference policy.

Prepaint timeout still returns the semantic document, including when illustrated hydration never arrives on a browser without WebGL. Later hydration cannot put an automatic overlay back over that document. An explicit request can retry. Visibility pauses transition clocks and deadlines. Old vignette events have no authority over the new presentation.

Validation used Node 24.19. All 289 relevant boot, original BootScreen, residency, chrome and capability tests pass. Targeted ESLint and `git diff --check` pass. Full `pnpm typecheck` has two downstream integration errors: `StacksHome.tsx:127` and `StacksCanvas.tsx:1057` pass the expanded status to the old `PerformanceDiagnosticEvent.boot_status` union in `src/lib/analytics.ts`. That union and corresponding server validation need `illustrated`, `dissolving` and `travelling`. They are outside this branch's file ownership. No browser tests ran.

This commit implements policy and browser adapters only. The integration owner supplies the persistent content UI, renderer registration and camera commands. No Field Notes discovery is added; automatic delivery and promotion fail quality-bar test 2 because they are not meaningful visitor actions.
