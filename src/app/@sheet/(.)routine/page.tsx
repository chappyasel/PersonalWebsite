import RoutinePage from "~/app/routine/page";

// The full routine page, presented in the sheet the segment layout owns.
export default function InterceptedRoutinePage() {
  return <RoutinePage />;
}
