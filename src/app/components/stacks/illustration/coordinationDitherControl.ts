import { createCoordinationGlobeDiagnosticsController } from "../scene/coordinationGlobeDiagnostics";

/** Approved 2D fringe motion. Debug changes last only for this page load. */
export const coordinationDitherController =
  createCoordinationGlobeDiagnosticsController();
