// The deep-link hooks moved to ~/components/daylight/hashTarget so the
// systems page's layer accordion opens on a hash the same way the routine's
// collapsible sections do. The routine keeps importing from here.
export {
  HashScrollSpacer,
  releaseHash,
  useHashTarget,
} from "~/components/daylight/hashTarget";
